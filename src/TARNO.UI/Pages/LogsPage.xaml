<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TARNO.UI.Pages.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:TARNO.UI.ViewModels"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16" Spacing="4">
            <TextBlock Text="Logs" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
            <TextBlock Text="Live-Logs und Fehlerdetails aus dem Backend." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource GlassCardStyle}">
            <ListView ItemsSource="{x:Bind ViewModel.Logs}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:LogEntryViewModel">
                        <Grid Padding="0,6">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="{x:Bind FormattedTimestamp}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                <TextBlock Text="{x:Bind Level}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextAccentBrush}" />
                                <TextBlock Text="{x:Bind Module}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                            </StackPanel>
                            <TextBlock Grid.Row="1" Text="{x:Bind Message}" TextWrapping="Wrap" Foreground="{StaticResource TextPrimaryBrush}" FontFamily="Consolas" FontSize="12" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
